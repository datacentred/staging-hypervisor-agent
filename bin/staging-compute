#!/usr/bin/ruby

# Add our local library paths to the search path
library_path = Dir[
  File.expand_path('../../lib', __FILE__)
]

$LOAD_PATH.unshift(*library_path)

# Run the staging-compute application
require 'staging-compute/staging-compute'

StagingCompute.run!
